# Docker Compose environment variables for local development
# Copy this file to .env in the same directory and replace placeholders as needed.

POSTGRES_USER=sleck
POSTGRES_PASSWORD=sleckdev
POSTGRES_DB=sleck
DATABASE_URL=postgres://sleck:sleckdev@db:5432/sleck
VITE_BACKEND_BASE_URL=http://localhost:3001
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key
